name: release-deb

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system tools
        run: |
          sudo apt-get update
          sudo apt-get install -y rsync dpkg-dev libcairo2

      - name: Install package and run checks
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pillow cairosvg
          python -m compileall core gui cli
          pytest -q

      - name: Build .deb
        run: |
          ./packaging/build_deb.sh "${GITHUB_REF_NAME#v}"

      - name: Smoke check generated icon set
        run: |
          test -f dist/iconset/linux/gdlex-pct-validator-256.png
          test -f dist/iconset/linux/gdlex-pct-validator.svg

      - name: Create GitHub Release and upload .deb
        uses: softprops/action-gh-release@v2
        with:
          name: GD LEX PCT Validator ${{ github.ref_name }}
          files: dist/*.deb
